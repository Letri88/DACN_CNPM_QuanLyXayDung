@model IEnumerable<DACN_CNPM_QuanLyXayDung.Models.Project>

@{
    ViewData["Title"] = "Quản lý Dự án";
}

<div class="p-8 space-y-8">
    <div class="glass-panel rounded-2xl overflow-hidden">
        <div class="p-6 border-b border-white/10 flex justify-between items-center">
            <h3 class="text-lg font-bold text-white">Danh sách Dự án</h3>
            <a asp-action="Create" class="px-4 py-2 bg-primary/20 text-primary hover:bg-primary/30 font-bold rounded-xl transition-colors text-sm flex items-center gap-2">
                <span class="material-icons text-sm">add</span> Thêm mới
            </a>
        </div>
        <div class="overflow-x-auto">
            <table class="w-full text-left">
                <thead>
                    <tr class="text-slate-500 text-xs uppercase tracking-wider bg-white/5">
                        <th class="px-6 py-4 font-semibold">Tên dự án</th>
                        <th class="px-6 py-4 font-semibold">Quản lý dự án</th>
                        <th class="px-6 py-4 font-semibold">Tiến độ</th>
                        <th class="px-6 py-4 font-semibold">Ngân sách</th>
                        <th class="px-6 py-4 font-semibold">Ngày bắt đầu</th>
                        <th class="px-6 py-4 font-semibold">Ngày kết thúc</th>
                        <th class="px-6 py-4 font-semibold">Trạng thái</th>
                        <th class="px-6 py-4 font-semibold text-right">Thao tác</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-white/5 text-slate-300 text-sm">
                    @foreach (var item in Model) {
                        <tr class="hover:bg-white/5 transition-colors">
                            <td class="px-6 py-4 font-semibold text-white">@Html.DisplayFor(modelItem => item.ProjectName)</td>
                            <td class="px-6 py-4">
                                @{
                                    var roleName = item.Manager?.Role?.RoleName?.Trim().ToLower() switch {
                                        "admin" => "Quản trị viên",
                                        "project manager" => "Quản lý dự án",
                                        "engineer" => "Kỹ sư",
                                        "warehouse keeper" => "Thủ kho",
                                        _ => item.Manager?.Role?.RoleName
                                    };
                                }
                                <div class="flex flex-col">
                                    <span>@(item.Manager?.FullName ?? "Chưa rõ")</span>
                                    @if (!string.IsNullOrEmpty(roleName)) {
                                        <span class="text-[10px] text-slate-400">@roleName</span>
                                    }
                                </div>
                            </td>
                            @{
                                var percent = item.Stages != null && item.Stages.Any() ? item.Stages.Average(t => t.PercentComplete ?? 0) : 0;
                            }
                            <td class="px-6 py-4 min-w-[140px]">
                                <div class="flex items-center gap-3">
                                    <div class="flex-1 h-1.5 bg-white/10 rounded-full overflow-hidden">
                                        <div class="h-full bg-primary" style="width: @Math.Round((double)percent)%"></div>
                                    </div>
                                    <span class="text-xs font-medium text-slate-300">@Math.Round((double)percent)%</span>
                                </div>
                            </td>
                            <td class="px-6 py-4">@(item.Budget?.ToString("N0"))</td>
                            <td class="px-6 py-4">@(item.StartDate?.ToString("dd/MM/yyyy") ?? "-")</td>
                            <td class="px-6 py-4">@(item.EndDate?.ToString("dd/MM/yyyy") ?? "-")</td>
                            <td class="px-6 py-4">
                                @if(item.Status == "Planned" || item.Status == "Pending") {
                                    <span class="px-2.5 py-1 rounded-full text-[10px] font-bold uppercase bg-amber-500/10 text-amber-400 border border-amber-500/20">@item.Status</span>
                                } else if (item.Status == "Active" || item.Status == "In Progress") {
                                    <span class="px-2.5 py-1 rounded-full text-[10px] font-bold uppercase bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">@item.Status</span>
                                } else {
                                    <span class="px-2.5 py-1 rounded-full text-[10px] font-bold uppercase bg-white/10 text-slate-300 border border-white/20">@item.Status</span>
                                }
                            </td>
                            <td class="px-6 py-4 text-right flex justify-end gap-2">
                                <a asp-action="Edit" asp-route-id="@item.ProjectId" class="w-8 h-8 rounded-lg bg-white/5 hover:bg-white/10 flex items-center justify-center text-slate-300 transition-colors" title="Sửa">
                                    <span class="material-icons text-sm">edit</span>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.ProjectId" class="w-8 h-8 rounded-lg bg-white/5 hover:bg-white/10 flex items-center justify-center text-slate-300 transition-colors" title="Chi tiết">
                                    <span class="material-icons text-sm">visibility</span>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.ProjectId" class="w-8 h-8 rounded-lg bg-red-500/10 hover:bg-red-500/20 flex items-center justify-center text-red-400 transition-colors" title="Xóa">
                                    <span class="material-icons text-sm">delete</span>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
